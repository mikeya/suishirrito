<% if target.errors.any? %>
    <div class="alert alert-danger errors">
        <h3><%= pluralize(target.errors.count, "error") %>:</h3>
        <% target.errors.full_messages.each do |msg| %>
            <p><%= msg %></p>
        <% end %>
    </div>
<% end %>